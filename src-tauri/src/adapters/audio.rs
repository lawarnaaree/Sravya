// Audio playback adapter — symphonia decode + cpal output.
// Stub for Phase 0; implementation in Phase 1.

pub struct AudioEngine;

impl AudioEngine {
    pub fn new() -> Self {
        Self
    }
}
